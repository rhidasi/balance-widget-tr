<FrameLayout
	xmlns:android="http://schemas.android.com/apk/res/android"
	android:id="@+id/widget_layout"
	android:layout_width="match_parent"
	android:layout_height="match_parent"
	android:layout_margin="@dimen/widget_margin"
	android:clickable="true"
	android:focusable="true">

	<ImageView
		android:layout_width="match_parent"
		android:layout_height="match_parent"
		android:src="@mipmap/ic_widget_dark"
		android:contentDescription="@null"
		android:scaleType="fitXY"
		android:alpha="0.8"/>

	<TextView
		android:id="@+id/widget_text"
		android:layout_width="match_parent"
		android:layout_height="match_parent"
		android:layout_marginTop="@dimen/widget_text_margin"
		android:layout_marginLeft="@dimen/widget_text_margin"
		android:gravity="center"
		android:text="@string/widget_text_loading"
		android:textColor="#FFF"
		android:textStyle="bold"
		android:textSize="@dimen/widget_text_size"/>

	<ImageButton
		android:id="@+id/widget_settings"
		android:layout_width="wrap_content"
		android:layout_height="wrap_content"
		android:padding="@dimen/widget_settings_padding"
		android:layout_gravity="right|bottom"
		android:background="@android:color/transparent"
		android:src="@drawable/ic_settings_black"
		android:tint="#FFF"
		android:contentDescription="@string/settings"/>

</FrameLayout>